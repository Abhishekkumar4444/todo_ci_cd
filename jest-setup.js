// A Jest setup file is used to configure and initialize the testing environment before the tests are executed.
// It allows you to perform any global configuration or initialization needed for your tests,
// such as mocking modules, adding custom matchers, or setting up global variables.

import '@testing-library/react-native/extend-expect';

// Mocking react-native-reanimated
// reanimated if using animation
